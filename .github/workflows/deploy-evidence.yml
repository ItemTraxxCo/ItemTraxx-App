name: Deploy Evidence

permissions: {}

on:
  workflow_run:
    workflows:
      - Deploy Supabase Functions
      - Deploy Cloudflare Worker
    types:
      - completed

jobs:
  evidence:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ItemTraxxCo/devops/.github/workflows/reusable-deploy-evidence.yml@6bcf366a32a1c5a5198b014648fe8e4133828e30
    with:
      run_id: ${{ format('{0}', github.event.workflow_run.id) }}
      sha: ${{ github.event.workflow_run.head_sha }}
      workflow_name: ${{ github.event.workflow_run.name }}
      surfaces: ${{ github.event.workflow_run.name == 'Deploy Supabase Functions' && 'supabase-functions' || 'cloudflare-worker' }}
    secrets:
      AI_API_KEY: ${{ secrets.AI_API_KEY }}
