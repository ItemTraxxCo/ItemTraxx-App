name: PR Risk Review

permissions:
  contents: read
  issues: write
  pull-requests: write

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

jobs:
  risk-review:
    if: ${{ !github.event.pull_request.draft && github.actor != 'dependabot[bot]' }}
    uses: ItemTraxxCo/devops/.github/workflows/reusable-pr-risk-review.yml@90a58846345e8fc3adfd204a18f762961b052ec3
    with:
      pr_number: ${{ format('{0}', github.event.pull_request.number) }}
    secrets:
      AI_API_KEY: ${{ secrets.AI_API_KEY }}
